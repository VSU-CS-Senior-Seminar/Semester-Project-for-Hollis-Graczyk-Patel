<%= simple_form_for(@event_comment) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :content, label: "Comment" %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>